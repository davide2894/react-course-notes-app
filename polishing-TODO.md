TODO
[] scss -> group selectors in scss all over the proje
[] consider to split or isolate concers in js code logic (ex: methods non strictly aimed to render the component should be put in a specific module, living on its own)